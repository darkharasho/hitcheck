{
  "name": "hitcheck-croptool",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute hitcheck-crops --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
